# Roadmap

## Phase 0 - Governance
- Initialize startup kit
- Confirm scope boundaries
- Set release/deployment guardrails
- Completed 2026-06-15: workspace sync automation (`sync-workspace-github.ps1`) and existing-project onboarding kit (`init-existing-project-governance.ps1`)
- Completed 2026-06-16: global prompt protocol stabilized (`#bonjour`, `#go-bug`, `#go-ui`, `#go-compact`, `#bonne-nuit`) with session memory handoff.

## Phase 1 - MVP
- Define MVP scope
- Deliver first validated feature slice

## Gate Rule
Each phase ends with review -> explicit GO -> deployment.
